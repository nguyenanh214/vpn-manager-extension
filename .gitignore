# Không bao giờ commit certificate hay khoá riêng
*.pem
*.key
*.crt
*.ovpn
*.p12
*.pfx
*.cer
*.jks

# Rác
node_modules/
*.log
.DS_Store

# Cấu hình test cục bộ — chứa đường dẫn và IP riêng của từng máy
.env
